import { FullScreenComponent } from '../components/sales/full-screen';

export default function HomeAndShop({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      {children}
    </>
  );
}
